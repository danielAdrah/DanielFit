---
trigger: model_decision
description: when using command Line commands, never combine
 more than one command at once (in other words, do
 not use &&) and remember, you are on a windows
 machine.
---
when using command Line commands, never combine
 more than one command at once (in other words, do
 not use &&) and remember, you are on a windows
 machine.